# Regular expression to identify age of the a deceased patient in the notes
# Looks for numeric values either single or double digits following the pattern yo or y.o. in the notes
# Note the regular expression performs case insensitive matching
\b(?i)([9][0-9]|[1][0-9][0-9])(?=\syo\b|\sy\.o\.)
